@using System.Web.Http
@using System.Web.Http.Description
@using System.Collections.ObjectModel
@using DoctorDrive.API.Areas.HelpPage.Models
@model Collection<ApiDescription>

@{
    ViewBag.Title = "Welcome to Doctor Drive API";
    ILookup<string, ApiDescription> apiGroups = Model.ToLookup(api => api.ActionDescriptor.ControllerDescriptor.ControllerName);
}

<header>
    <div class="container-fluid">
        <div class="form-horizontal">
            <h1>@ViewBag.Title</h1>
        </div>
    </div>
</header>
<div id="body">
    <section class="row-fluid">
        <div class="span2">
            <h2>Introduction</h2>
            <p>
                Provide a general description of your APIs here.
            </p>
        </div>
    </section>
    <section class="content-wrapper main-content clear-fix">
        @foreach (var group in apiGroups)
        {
            @Html.DisplayFor(m => group, "ApiGroup")
        }
    </section>
</div>

@section Scripts {
    <link type="text/css" href="~/Areas/HelpPage/Content/css/HelpPage.css" rel="stylesheet" />
}